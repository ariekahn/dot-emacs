This is an attempt at a 'literate' emacs config.

This setup is largely centered around `org-mode`.

`init.el` is a stub that tangles and runs `literateconfig.org`,
which contains the rest of the setup.
